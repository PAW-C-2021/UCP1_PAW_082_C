@model IEnumerable<UTS_DataHadir.Models.KeteranganPembayaran>

@{
    ViewData["Title"] = "Keterangan Pembayaran";
}

<h1>Keterangan Pembayaran</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KetBayar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KetBayar)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKetBayar"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdKetBayar"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdKetBayar"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
